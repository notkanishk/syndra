import { WithdrawnAccess } from "@/components/review/WithdrawnAccess";

/**
 * Review › Withdrawn access. Access somebody took away that is still there —
 * beside Unexplained access, and never merged into it.
 */
export default function WithdrawnAccessPage() {
  return <WithdrawnAccess />;
}
