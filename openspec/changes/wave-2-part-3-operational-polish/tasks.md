# Wave 2 · Part 3 — Tasks

> Detailed step-by-step (TDD-ordered, with exact code blocks and commands) lives at [`docs/superpowers/plans/2026-05-25-wave-2-part-3-operational-polish.md`](../../../docs/superpowers/plans/2026-05-25-wave-2-part-3-operational-polish.md). The list below is the OpenSpec progress ledger. Plan task numbers are executed in size-ascending order; audit IDs are preserved on the right for traceability.

- [x] **Task 0** — Commit OpenSpec scaffolding (this directory + one spec delta + plan); no source code touched yet
- [x] **Task 1** — Delete the deferred zero-buffer loop in `sync/internal/worker/worker.go:191-195`; replace with a 2-line comment documenting the Go-string-immutability constraint that makes the zero a no-op. *(Audit: C10)*
- [x] **Task 2** — Lower `grantLookupMaxPages` from `100` to `10` in `backend/internal/handlers/zitadel_grant_lookup.go:15`; update the explanatory comment block to reflect the new sizing rationale (≈100 grants/user p99, 10 pages = 1000-grant headroom). *(Audit: B8)*
- [x] **Task 3** — `scripts/smoke-test-lxc.sh:11` probes `/healthz` instead of `/api/v1/bundles`; the bearer-token header is removed from the API check so the smoke test works on real OIDC LXC deployments. *(Audit: C9)*
- [x] **Task 4** — Rewrite the `.env.example:21-30` `EXPIRY_SCHEDULER_*` comment block to drop the "N > 1 replicas" framing — single-LXC deployment doesn't have multi-replica. *(Audit: D9)*
- [x] **Task 5** — Append `--- Sync Service / LLDAP ---` block to `.env.example` documenting all 12 sync env vars from `sync/internal/config/config.go`; add `SYNDRA_EXTERNAL_URL` and `ZITADEL_M2M_TOKEN` to the backend block (required at runtime but missing from template). *(Audit: D7)*
- [x] **Task 6** — Fold `zitadel/actions/PERMISSIONS.md` and `zitadel/actions/SIGNING_KEY.md` into `zitadel/actions/README.md` as `## Service-Account Permissions` and `## Signing Key Handling` sections; delete the two now-merged files; `EVENTS.md` stays separate. *(Audit: S3)*
- [x] **Task 7** — Replace `member: [""]` placeholder with `member: [p.cfg.BindDN]` at `sync/internal/ldap/client.go:170` (`EnsureGroup` `groupOfNames` creation path); add a unit test that asserts the bind DN appears in the AddRequest. *(Audit: C7)*
- [x] **Task 8** — Plumb `context.Context` through `withConn` in `sync/internal/ldap/client.go:73`; thread `ctx` from every caller (`EnsureUser`, `EnsureGroup`, `AddUserToGroup`, `RemoveUserFromGroup`, `SetUserPassword`, `Search`, etc.); add fail-fast `ctx.Err()` check before mutex acquisition, immediately after acquisition, and before the reconnect retry. The active LDAP op holding the mutex is NOT interrupted; queued goroutines fail at the post-acquisition checkpoint. Add a unit test that asserts a pre-cancelled context returns immediately without invoking `fn`. *(Audit: C8)*
- [x] **Task 9** — Wire `SYNC_RETRY_ATTEMPTS` and `SYNC_RETRY_BACKOFF` via `getEnvInt` / `getEnvDuration` in `sync/internal/config/config.go:42-43`; defaults `3` / `1s` preserved; add a unit test that asserts both env vars override defaults and absent env vars fall back. *(Audit: S4)*
- [x] **Task 10** — Extract `scripts/lib/load-env.sh` from `zitadel/actions/{register,rotate}.sh`; both callers replace their 22-line block with one `source` line; `zitadel-api.sh` is deliberately NOT extracted (single-consumer; design.md Decision 4). *(Audit: S1; S2 deferred)*
- [x] **Task 11** — Drop `mapping_rules.version` end-to-end (14 touch points). Backend: new migration `000014_drop_mapping_rules_version` (up + down); delete `UpdateMappingRule` in `db/rules.go`; remove `version` from `GetActiveMappingRules` `SELECT`/`Scan`; delete `handleUpdateMappingRule` in `handlers/rules.go:128-144`; unregister `PUT /api/v1/rules/mapping/{id}` in `router.go:60`; drop `dbUpdateMappingRule` injectable in `deps.go:44`; delete the three `TestHandleUpdateMappingRule_*` tests and shrink `resetRulesDeps` in `rules_test.go`; delete `Version` field from `models.MappingRule`; remove the Topology meta line in `services/views.go:700`. Frontend: delete `version: number` from `useMappingRules.ts:MappingRuleRow` (line 13) and `lib/types.ts:MappingRule` (line 59); delete the `useBumpMappingRule` mutation in `useMappingRules.ts:75-87`; in `app/policies/page.tsx` drop the `useBumpMappingRule` import (line 16), `bumpRule` binding (line 38), `handleBump` function (lines 45-49), version badge (line 96), and the "Bump version →" button row including its now-empty wrapper `<div>` (around lines 135-145). *(Audit: D4)*
- [x] **Task 12** — Verification gate: `go test ./... && go vet ./...` from `backend/` and `sync/`; `bun run lint && bun run test && bun run build` from `ui/`; migration round-trip (`up`, `down 1`, `up`) on a throwaway DB to validate the D4 down migration; `gofmt -d` scoped to this wave's touch set; `mcp__codebase-memory-mcp__detect_changes` + reindex affected scope; `openspec validate wave-2-part-3-operational-polish --strict` from the repo root. *(Note: the live migration round-trip was not executed — no Postgres/Docker available in the implementation environment. Statically validated instead: golang-migrate file naming + sequencing, up/down SQL, and the `000004` `ck_mapping_rules_version_positive` CHECK constraint which Postgres auto-drops with the column. Run `up`/`down 1`/`up` against a real DB before deploy.)*
- [x] **Task 13** — `feature-coverage.md` row updates (Versioned policies → *Removed; rely on `audit_logs`*; LDAP Sync — retry → *env-configurable*); `INDEX.md` change-log row appended for this wave
