# Wave 3 — Tasks

> Detailed step-by-step (TDD-ordered, with exact code blocks and commands) lives at [`docs/superpowers/plans/2026-07-08-wave-3-frontend-remainder-and-consolidation.md`](../../../docs/superpowers/plans/2026-07-08-wave-3-frontend-remainder-and-consolidation.md). The list below is the OpenSpec progress ledger. Tasks are size-ascending; audit IDs are on the right for traceability.

- [x] **Task 0** — Commit OpenSpec scaffolding (this directory: proposal, design, tasks, two spec deltas) + the plan doc. No source code touched yet.
- [x] **Task 1** — Add `- [ ] **Optional: ⌘K command palette**` bullet to `openspec/changes/syndra-core-architecture/ROADMAP.md` Phase 6 (after the Google Workspace poller bullet, line ~81). *(Audit: D2)*
- [x] **Task 2** — Delete dead `HasPendingDrift` + its doc comment from `backend/internal/db/drift.go:121-140`; confirm zero callers; `db.PendingOutboxAddExists` remains the successor. Run `go build ./... && go vet ./...`. *(Residual: R1)*
- [x] **Task 3** — Delete `fetchBundles`/`fetchMappingRules`/`fetchProjects`/`fetchAudit` from `ui/src/lib/api.ts` (lines 70/74/86/90); remove now-unused type imports (`AuditLog`, `Bundle`, `MappingRule`, `ProjectSummary`) from line 2; keep `fetchApplications`/`fetchWithAuth`/`fetchSystemMode`/`fetchCatalog`/`fetchServerJson`/`resolveAuthToken`. *(Audit: U4)*
- [x] **Task 4** — `ui/src/lib/session.ts:224` OIDC avatar fallback chain `name → email local-part → userId` via a small `avatarSeed()` helper; add a unit test asserting a blank-name OIDC payload yields non-empty initials from email/userId. *(Audit: U6)*
- [x] **Task 5** — Extract `scripts/lib/zitadel-api.sh` (superset copy incl. `ZITADEL_API_TOLERATE_404` branch) from `register.sh:140-175` / `rotate.sh:118-150`; both scripts `source "${REPO_ROOT}/scripts/lib/zitadel-api.sh"` after `API_BASE`/`TOKEN` are set; delete the inline copies. `bash -n` both scripts. *(Residual: R3; finishes S2)*
- [x] **Task 6** — Add Vitest coverage for the admin/member boundary (zero today), covering **both enforcement mechanisms** (design.md Decision 8): `ui/src/middleware.test.ts` (redirect for `role:"user"` on **each** `ADMIN_ONLY_PATHS` entry, stale-demo clearing with `ZITADEL_DOMAIN` set, expired-OIDC → `/login`, valid-on-`/login` → `/`); `ui/src/app/__tests__/admin-page-guards.test.ts` (page-level server guards on `/operations`, `/operations/cascades`, `/governance/pending`, `/governance/drift`, `/grants` — member → `redirect("/")`, no session → `redirect("/login")`, admin → no redirect); `ui/src/app/api/proxy/[...path]/route.test.ts` (member self-scoping allow/deny, allowlist GET/POST enforcement incl. member `GET /bundles` → 403, `requester_id` injection + list filtering). `/zitadel`'s guard test is added in Task 8. Use `test-utils/proxyFetch.ts`, `vi.mock("next/headers")`, `vi.mock("@/lib/session")`, `vi.mock("next/navigation")`. *(Audit: U7)*
- [x] **Task 7** — Replace `ui/src/lib/queries/useNameResolver.tsx` (409-line rAF batcher) with a full-catalog resolver: **two independent, non-shared queries** — `GET /api/v1/catalog` (users+projects+roles) and `GET /api/v1/bundles` (`retry:false`) — on `<NameResolverProvider>` mount, build the `Map`s, resolvers become synchronous `Map` reads with **per-source `resolved` flags**. Independent because the provider is global and member `GET /bundles` is a 403 by proxy policy — a shared/`Promise.all` fetch would sink user/project/role resolution for members; bundles must soft-fail in isolation (design.md Decision 1). Preserve the exact interface (`NameResolverProvider`, `useNameResolver()`, `ResolveResult<T>` tri-state, no-provider fallback, `prefetch` no-op). Invalidate catalog key on user create/delete. Extend `lib/queries/__tests__/useNameResolver.test.tsx` for: loading (`resolved:false`), resolved-hit, resolved-miss (`resolved:true,value:undefined`), **member-context bundles-403 leaves user/project/role resolving while bundle falls back**, post-invalidation refetch. *(Audit: U1)*
- [x] **Task 8** — Add `preserveErrorBody?: boolean` to `request<T>` options in `ui/src/lib/api-client.ts` (branch at the non-2xx site: return parsed body instead of throwing). Split `ui/src/app/zitadel/page.tsx` (955 lines) into `ui/src/components/zitadel/{Health,Rotation,Projects,Users,AllGrants}.tsx` + a `ZitadelDiagnostics.tsx` client island. Replace `apiGet`/`apiSend`/`apiGetDiagnostic` with `request<T>` (health probe passes `preserveErrorBody:true`). Behavior unchanged (incl. `grantFlash` async-status handling). **Also close the P2 gating gap:** rewrite `page.tsx` as an async server component with the standard admin guard (`getSession()` → `redirect("/login")`/`redirect("/")`, mirroring `app/grants/page.tsx`) rendering `<ZitadelDiagnostics/>` — today `/zitadel` is gated by neither middleware nor page guard. Add a `request` unit test for the `preserveErrorBody` branch; add the `/zitadel` case to `admin-page-guards.test.ts`; keep existing zitadel page tests green. *(Audit: U5; +P2 security fix)*
- [x] **Task 9** — Inline §5.2 pending tag in `ui/src/components/zitadel/Users.tsx` (post-split): `useMemo` a `Set<"user|project|role">` of pending adds from `usePendingPropagations()` (match `role_keys[]` membership, `op_type:"add"`); render `[⏱ Awaiting Zitadel]` + Resume-to-dashboard link next to matching grants, Material `tertiary-container` tokens, `prefers-reduced-motion`-safe. Add a component test asserting the tag renders for a grant with a pending add and not otherwise. *(Residual: R2; design §5.2)*
- [x] **Task 10** — Drop `location` end-to-end (keep `title`/`team`). Backend: remove `Location` from `models.go:69`, `directory/zitadel.go:197-198,491`, `demo/catalog.go:10-14`, `handlers/profile.go`; fix `directory_test.go:492-493`, `profile_test.go:46,63`. Frontend: remove from `types.ts:44`, `session.ts:14,40,68/80/92/104/116,165,223`, `oidc.ts:227,242,253`, `auth/callback/route.ts:139`, `app/page.tsx:56`, `login/page.tsx:127`; fix `session.test.ts`. Specs: `feature-coverage.md:24` row + `live-directory-identity-completeness` (owning capability); shipped wave-1 spec left as historical record. *(Audit: D6)*
- [x] **Task 11** — Verification gate: `go test ./... && go vet ./...` from `backend/`; `bun run lint && bun run test && bun run build` from `ui/`; `bash -n` on both edited scripts; `mcp__codebase-memory-mcp__detect_changes` + reindex affected scope; `openspec validate wave-3-frontend-remainder-and-consolidation --strict` from repo root.
- [x] **Task 12** — Consolidation pass: append a Wave 3 change-log row to `openspec/INDEX.md`; drop `location` from the `feature-coverage.md:24` user-management identity row; confirm `ROADMAP.md` (⌘K stub present from Task 1) and `syndra-core-architecture/design.md` (two-layer doctrine, no ⌘K) read coherently against shipped reality. *(§7 consolidation)*
