# Wave 2 · Part 4 — Tasks

> This is the OpenSpec progress ledger for the whole change. **Sub-phase 1** has a detailed step-by-step (TDD-ordered, exact code + commands) plan at [`docs/superpowers/plans/2026-06-09-wave-2-part-4-phase-1-outbox.md`](../../../docs/superpowers/plans/2026-06-09-wave-2-part-4-phase-1-outbox.md). Sub-phases 2 and 3 are task-level here and get their own writing-plans pass when sub-phase 1 lands. Plan task numbers execute in size-ascending order within each sub-phase; audit IDs are preserved for traceability.

## Sub-phase 1 — Outbox & operator-confirmed propagation (B4/D3) — *detailed plan exists* — **IMPLEMENTED**

- [x] **Task 0** — Commit OpenSpec scaffolding (this directory + three spec deltas + the sub-phase-1 plan); no source touched
- [x] **Task 1** — Migration `000015_zitadel_propagation_outbox` (up + down): `pending_zitadel_propagations` table + status/created_at index; `direct_role_grants.source` (full 5-value CHECK enum) + `source_ref`, existing rows backfill to `source='direct'`. Down drops both columns + the table. *(B4/D3)*
- [x] **Task 2** — `models.PendingPropagation` struct; add `Source`/`SourceRef` to `models.DirectGrant`. (Also added `models.PendingPropagationSummary` + the `GovernanceSummary.PendingPropagation` field here.) *(B4/D3)*
- [x] **Task 3** — `db/propagations.go`: `InsertPendingPropagation` / `ClaimPendingPropagations` (`FOR UPDATE SKIP LOCKED`) / `MarkPropagationApplied` / `MarkPropagationFailed` / `RequeuePropagation` / `GetPendingPropagations` / `CountPendingPropagations` / `PruneTerminalPropagations`; `db.GrantIndexHasRole` added to `webhooks.go`. Idempotency keys minted from `crypto/rand` (no new uuid dep — see notes). **Test strategy adapted:** the `db` package has no live-DB harness (it is tested only via migration-coherence guards, e.g. `webhooks_test.go`), so claim/transition semantics are covered by the injectable `services/propagation` drain tests rather than live SQL; the SQL layer itself gets a migration-coherence guard (`propagations_migration_test.go`) asserting the CHECK enums match the literals the Go code writes. *(B4/D3)*
- [x] **Task 4** — `db.EnqueueDirectGrantPropagation` (one `pgx.Tx`: upsert `direct_role_grants` per role + insert `audit_logs` + insert outbox row; revoke intents skip the ledger upsert). `enqueueTx(ctx,p,key)` seam for a fixed key. **Deviation:** the existing `UpsertDirectGrant` signature was left unchanged (it serves the separate access-request-approval flow); the enqueue uses its own tx-scoped upsert that sets `source`/`source_ref`, and `GetDirectGrantsForUser`/`GetAllDirectGrants` now surface them via `COALESCE`. *(B4/D3)*
- [x] **Task 5** — `services/propagation/{drain,deps}.go`: `Drain(ctx)` — health pre-flight, claim, already-exists check (grant index → one live `ListUserGrants` per row), per-row dispatch by `op_type`, ACK classification (`2xx`/`AlreadyExists`(409)→applied; `4xx` except `429`/`408`→failed; `5xx`/timeout/`429`/`408`→requeue with `OUTBOX_MAX_RETRIES` halt), and a drain-tail opportunistic prune of terminal rows older than `OUTBOX_RETENTION_DAYS` (default 30). Classifier reads the new typed `zitadel.StatusError.Code` (HTTP status threaded through `doRequest`), not the error string. Tests cover each ACK class (incl. `429`/`503`/`408`→transient and `409`→applied), 4xx-doesn't-halt-batch, retry-budget halt, the already-exists/revoke short-circuits, and the prune. *(B4/D3)*
- [x] **Task 6** — Rewired `access.go:handleUpsertUserDirectGrant` (`POST /api/v1/users/{id}/grants`) to `EnqueueDirectGrantPropagation`; `202 {outbox_id, idempotency_key, status}`. `?apply=true` triggers an inline drain. Audit write moved into the transactional enqueue. *(B4/D3)*
- [x] **Task 7** — Rewired `discovery.go` `handleAssignZitadelGrant` / `handleUpdateZitadelGrant` / `handleRemoveZitadelGrant` through the canonical path: add→`add`, update→`replace`, remove→`revoke`. `update`/`remove` resolve `grantId→(project,roles)` via `db.GetGrantIndex` (live `ListUserGrants` fallback). `202` response. `/zitadel/*` URLs preserved. The handler-side Zitadel grant closures were **deleted** (now dead — the drain owns them), making the B4/D3 boundary structural. *(B4/D3)*
- [x] **Task 8** — `POST /api/v1/propagations/drain` ("Resume now") + `GET /api/v1/propagations` (per-row worklist); extended governance summary with a `pending_propagation` block (count + zitadel reachable; reachability is the cheap `MgmtClient != nil` signal, not a per-load ping). Operator-auth router wiring beside reconciliation. *(B4/D3)*
- [x] **Task 9** — Frontend Pending Propagation surfaces: `usePropagation` hooks (`usePendingPropagations` + `useDrainPropagations`); `pending_propagation` added to the `GovernanceSummary` type+mapper; nested `Pending [N]` sidebar item under Operations; dismissible dashboard `PendingCallout` (reachability-gated `Resume now`) + render tests; `app/zitadel/page.tsx` grant callers read the `202 {status}` shape via `grantFlash`. Added a `/governance/pending` worklist page (server-gated + client island) so the sidebar link resolves. *(B4/D3)*
- [x] **Task 10** — Sub-phase-1 verification gate: `go test ./... && go vet ./...` (backend, all pass / vet clean); `bun run test && lint && build` (ui, 83 tests pass / no lint errors / build OK); `gofmt` clean on the touch set; `000015` round-trip validated statically via the migration-coherence guard (no throwaway Postgres in this env, as Wave 2 · Part 3 Task 12 did); `openspec validate … --strict` passes; codebase-memory reindex. *(B4/D3)*
- [x] **Task 10a** — Sub-phase-1 post-review hardening (four drain/outbox correctness fixes). *(B4/D3)*
  - **Persist failures never reported as success** — the dispatch switch now checks the `markApplied`/`markFailed`/`requeue`/reconcile errors it previously discarded (`_ =`); a failed state write is counted under the new `DrainResult.Errored`, leaves the row `in_flight`, and does NOT halt the batch. (`drain.go`; `persistErr`)
  - **`in_flight` rows are reclaimed** — `ClaimPendingPropagations` now claims `status IN ('pending','in_flight')` (was `'pending'` only), matching the worklist/count set and the design's crash-recovery scenario; source-coherence guard `TestClaimPendingPropagations_ReclaimsInFlight`. (`db/propagations.go`)
  - **Applied revoke/replace reconcile the ledger** — new `db.ReconcileLedgerOnApplied` (revoke → delete named triples; replace → delete `source='direct'` rows not in the new set) wired into `applyRow`, run BEFORE the terminal mark, on both the dispatch and short-circuit paths. (`db/propagations.go`, `drain.go`, `deps.go`)
  - **Replace requires an EXACT match to short-circuit** — `alreadyExists` split `add` (superset OK) from `replace` (live set must equal desired exactly), so a superseded role still in Zitadel no longer skips `UpdateUserGrant`. (`drain.go`)
  - Tests: 8 new `services/propagation` drain tests + the `db` claim guard; full backend suite green (`go test ./...`, `go vet ./...`, `gofmt`).
- [x] **Task 10b** — Second-round review hardening (concurrency + reporting fidelity). *(B4/D3)*
  - **Drains serialized by advisory lock** — new `db.TryAcquireDrainLock` (`pg_try_advisory_lock` on a dedicated pooled conn) wired as the `acquireDrainLock` injectable; `Drain` takes the lock first and returns `Halted{Reason:"drain_in_progress"}` if another drain holds it. This is what makes Task 10a's `in_flight` reclaim safe against concurrent "Resume now"/inline-apply drains (a `revoke` double-dispatched would otherwise 404→fail the row). (`db/propagations.go`, `drain.go`, `deps.go`)
  - **Inline `?apply=true` reports THIS row's status** — new `db.GetPropagationStatus`; `handleUpsertUserDirectGrant` reads back the requesting `outbox_id`'s status after the batch drain instead of inferring `applied` from the batch aggregate (which could belong to an older row). (`db/propagations.go`, `handlers/access.go`, `handlers/deps.go`)
  - **`errored` surfaced in the UI** — added `errored` to the `DrainResult` type; new presentational `DrainResultBanner` renders on the pending worklist after a drain, raising an explicit alert ("could not be recorded — resume again to retry") when `errored > 0`, so a `200` with stuck rows is no longer silent. (`ui/lib/queries/usePropagation.ts`, `ui/components/propagation/DrainResultBanner.tsx`, `PendingPropagationsClient.tsx`)
  - Tests: 4 new drain-lock tests + 2 inline-apply handler tests (backend, 378 pass); 5 `DrainResultBanner` tests (ui, 88 pass); `go vet`/`gofmt` clean, `bun lint`/`build` clean.
- [x] **Task 10c** — Route access-request approval through the outbox (third review round). *(B4/D3)*
  - `handleResolveAccessRequest` (`status=approved`) now calls `EnqueueDirectGrantPropagation` (`op_type='add'`, `source='direct'`, `source_ref=<request id>`) instead of the bare `UpsertDirectGrant`, then applies inline (best-effort drain). Previously an approved grant wrote only the ledger row — invisible to the Pending UI, unprojected to Zitadel, and destined to re-surface as `mkauth_only` drift in sub-phase 2. Access stays immediate because the claim compiler reads `direct_role_grants`. (`handlers/access.go`)
  - Decision (operator-chosen): **enqueue + apply inline** — the approval is the operator's confirmation.
  - Tests: consolidated the two legacy approval tests into `...ApprovedEnqueuesGrantAndRebuildsCache` (asserts enqueue params, source_ref, inline drain, no bare upsert, audit, cache rebuild, expiry) and hardened the rejected-path test to assert no enqueue. Full backend suite green (377 pass), `go vet`/`gofmt` clean.
- [x] **Task 10d** — Fourth review round: approval atomicity + targeted inline-apply. *(B4/D3)*
  - **Resolution + enqueue are one conditional transaction** — new `db.ApproveRequestAndEnqueue` (`UPDATE access_requests … WHERE status='pending'` then `enqueueWrites` on the same `pgx.Tx`), replacing the resolve-then-enqueue split that could strand an approved-but-ungranted request. `ResolveAccessRequest` (reject path) now also guards on `status='pending'`. Both return the new `db.ErrRequestNotPending` sentinel → handler maps to `409`, closing the concurrent approve/reject race. Extracted `enqueueWrites(tx,…)` from `enqueueTx` so the enqueue writes compose into an outer tx. (`db/access_requests.go`, `db/propagation_enqueue.go`, `handlers/access.go`, `handlers/deps.go`)
  - **Targeted inline-apply** — new `propagation.DrainOne(outboxID)` + `db.ClaimPropagationByID`; both `?apply=true` (`handleUpsertUserDirectGrant`) and approval now drain ONLY their own row, not the global batch, so applying one grant never projects unrelated queued mutations. `Drain`'s loop body was extracted into a shared `processRow` so batch and targeted drains classify/apply identically; `DrainOne` reuses the advisory lock + reachability pre-flight. (Operator-chosen: targeted for both inline-apply paths.) (`services/propagation/{drain,deps}.go`, `db/propagations.go`)
  - Tests: 4 new `DrainOne` tests (target-only, not-found no-op, lock-serialized, offline-halt); rewrote the approval test for the combined tx + targeted drain; added approve-race and reject-race `409` tests; updated the two `?apply=true` tests to assert targeted drain. Full backend suite green (383 pass), `go vet`/`gofmt` clean.
- [x] **Task 10e** — Fifth review round: rebuild the compiled cache BEFORE the inline drain. *(B4/D3)*
  - Both the approval path and `?apply=true` previously drained (best-effort, on the request context) and *then* rebuilt the requester cache — so a slow/canceled Zitadel call could starve the rebuild and leave access ineffective, contradicting the "effective immediately regardless of projection outcome" guarantee. Reordered both to rebuild from the just-committed ledger row first, then run the targeted inline drain. (`handlers/access.go`)
  - Tests: 2 new call-ordering tests (`...RebuildsCacheBeforeInlineDrain` for approval and `?apply=true`) asserting rebuild precedes drain. Full backend suite green (385 pass), `go vet`/`gofmt` clean.
- [x] **Task 10f** — Detach the compiled-cache rebuild from the request lifecycle. *(B4/D3)*
  - Follow-up to 10e's residual: reordering stopped the *drain* from starving the rebuild, but the rebuild still inherited `r.Context()`, so a client disconnect *before* the rebuild started could still skip it. New `rebuildUserCacheDetached` runs it on `context.WithTimeout(context.WithoutCancel(r.Context()), 10s)` — after commit the grant is durable, so the rebuild that makes access effective must complete regardless of client lifecycle. Applied to both grant paths (`handleUpsertUserDirectGrant`, `handleResolveAccessRequest`). It stays synchronous (handler still finishes the rebuild before responding); it just no longer inherits request cancellation. (`handlers/access.go`)
  - Tests: 2 new `...RebuildDetachedFromClientCancel` tests — issue the request with an already-canceled context and assert the cache rebuild still runs AND receives a non-canceled context. Full backend suite green (387 pass), `go vet`/`gofmt` clean.

## Sub-phase 2 — Drift detection & triage (B2, C6) — *detailed plan exists* — [`docs/superpowers/plans/2026-07-06-wave-2-part-4-phase-2-drift.md`](../../../docs/superpowers/plans/2026-07-06-wave-2-part-4-phase-2-drift.md)

- [x] **Task 11** — Migration `000016_drift_queue` (up + down): `drift_items` (+ partial-unique pending index, keyed `(user,project,drift_type,role_keys)`) + `external_grant_exclusions`. *(B2)*
- [x] **Task 12** — `db/drift.go` + `db/exclusions.go` repositories (upsert/list/resolve drift; insert/check exclusions); models; three atomic tx triage helpers reusing `enqueueWrites`; migration-coherence guard. *(B2)*
- [x] **Task 13** — Drop `reconciliationSafetyCap` 10 000 → 2 000; pure `services/expected.go` (`ExpectedViaRule`/`IsExcluded`) filters rule-derived/excluded out of `OnlyInZitadel` (endpoint + sweep share it); `computeReconciliationDiff` kept pure; lookups 500 on error. *(B2)*
- [x] **Task 14** — `services/drift/{scheduler,sweep,deps}.go` (mirror `services/expiry/`): scheduled sweep on `DRIFT_RECONCILIATION_INTERVAL_HOURS` (default 6); `zitadel_only → drift_items.upsert`; `mkauth_only → outbox re-enqueue`. Wire into `main.go` beside the expiry scheduler with graceful `Done()`. *(B2, C6)*
  - `deps.go` injects `db.GetAllDirectGrants(ctx, false)` (active-only bool arg the design doc's shorthand omitted), `db.GetActiveMappingRules`, `db.GetExclusions`, `db.UpsertDriftItem`, `db.HasPendingDrift`, `db.InsertPendingPropagation`, `zitadel.MgmtClient` reachability + `ListAllGrants` paging, and the new `db.NewOutboxIdempotencyKey` (exported wrapper around the existing `newOutboxIdempotencyKey` crypto/rand v4 minter, added for cross-package use — `EnqueueDirectGrantPropagation`'s call site untouched).
  - `sweep.go` has its own small triple-granularity diff (does NOT import `handlers.computeReconciliationDiff`, avoiding a handlers→drift→handlers import cycle): `zitadel_only` = live Zitadel role not covered by a direct grant, `expected_via_rule`, or an exclusion → `UpsertDriftItem(...,"reconciliation_sweep","zitadel_only")`; `mkauth_only` = direct grant absent from Zitadel, skipped when `HasPendingDrift(...,"mkauth_only")` is already true → re-enqueued via `InsertPendingPropagation("add",...)`. Paginates `ListAllGrants` capped at `driftSafetyCap=2_000` (mirrors B2's `reconciliationSafetyCap`). Halts cleanly (`Halted:true, Reason:"zitadel_offline"`) when `zitadel.MgmtClient` is nil; aborts (not degrades) on rules/exclusions lookup failure to avoid false-positive drift.
  - `scheduler.go` mirrors `expiry.Scheduler` exactly: immediate sweep on boot, ticks on `DRIFT_RECONCILIATION_INTERVAL_HOURS` (default 6h), panic-recovering `runOnce`, graceful `Done()` channel.
  - `main.go` wires `driftSched` beside the expiry `sched` block (construct → `go Start(ctx)` → join `Done()` in the shutdown `select`) and adds `driftSchedulerEnabled()` / `driftInterval()` env helpers beside `schedulerEnabled()`/`schedulerInterval()`. `.env.example` documents `DRIFT_SCHEDULER_ENABLED` / `DRIFT_RECONCILIATION_INTERVAL_HOURS`.
  - Tests: 6 new table-style sweep tests in `sweep_test.go` (injectable swap pattern, no live DB/Zitadel) — unexplained Zitadel grant → drift; rule-derived grant → no drift; mkauth_only direct grant → re-enqueue; pending-drift triple skips re-enqueue; Zitadel offline → clean halt; excluded grant → no drift. Full backend suite green, `go vet`/`gofmt` clean.
  - Note: Tasks 11–13 above are also implemented in code (`db/drift.go`, `db/exclusions.go`, `services/expected.go`, `reconciliationSafetyCap=2_000`) but their checkboxes were left stale; out of scope for this task, flagged here for a follow-up tick.
  - **Post-review correction (2026-07-06):** the `HasPendingDrift(...,"mkauth_only")` guard above was DEAD — nothing ever writes a drift row with `detection_type='mkauth_only'` (drift rows are `zitadel_only` only; `mkauth_only` is purely an outbox re-enqueue path), so every direct grant that stayed missing from Zitadel across sweeps minted a fresh duplicate `pending_zitadel_propagations` "add" row every tick (6h default). Replaced with `db.PendingOutboxAddExists(ctx,user,project,role)` — a real check for an undrained ('pending'/'in_flight') `op_type='add'` outbox row for the triple — so a persistently-missing grant is only queued once until that row drains or terminates. Removed the now-unused `hasPendingDrift` injectable from `services/drift/deps.go` (the underlying `db.HasPendingDrift` function is untouched; no other caller). Renamed `TestSweep_MkauthOnlySkipsReEnqueueWhenPendingDrift` → `...WhenPendingOutboxAdd` in `sweep_test.go`. On a lookup error the sweep now logs and proceeds to re-enqueue (risk of a rare duplicate) rather than silently skipping a real drift signal.
- [x] **Task 15** — Real-time webhook drift: in `webhook.go`, a surviving (non-self) `grant_added` unmatched by `external_grant_exclusions` + expected set (`services.UserExpectsRole` reusing `collectUserRoles`) inserts `drift_items(detection_source='webhook', drift_type='zitadel_only')`, best-effort/non-fatal. **Scope:** `grant_removed`→`mkauth_only` deliberately deferred to the sweep backstop (avoids webhook/sweep double-handling); C6 cache-miss gap likewise sweep-backstopped. Tests. *(C6)*
- [x] **Task 16** — Triage API: `GET /api/v1/governance/drift` (filters: user/project/source); per-item `POST …/attribute` (`external_backfill`|`bundle`|`rule`, source-remap validation), `POST …/revoke` (enqueue `op_type='revoke'`, mark `revoked` when outbox `applied`), `POST …/mark-external` (insert exclusion); bulk-attribute endpoint; `[Reconcile now]`. *(B2)*
  - `handlers/drift.go` (new): `handleListDrift`, `handleAttributeDrift`/`attributeOneDrift`, `handleRevokeDrift`, `handleMarkDriftExternal`, `handleBulkAttributeDrift`, `handleReconcileDrift`, `bundleHasRole`, `errDriftBadRemap`, `writeDriftActionError` (`db.ErrDriftNotPending`→409, `errDriftBadRemap`→400, else 500). Each triage action delegates to the Task-12 atomic `db.AttributeDriftAndEnqueue` / `db.RevokeDriftAndEnqueue` / `db.MarkDriftExternalTx` helpers (claim + side-effect in ONE tx) so a lost concurrent-triage race 409s with nothing written on either side; revoke drains its own outbox row (`propagation.DrainOne`) best-effort after commit.
  - `handlers/deps.go` injects `dbGetDriftItems`, `dbGetDriftItem`, `dbAttributeDriftAndEnqueue`, `dbRevokeDriftAndEnqueue`, `dbMarkDriftExternalTx`, `svcDriftSweep=drift.Sweep`, `svcDrainOne=propagation.DrainOne`, `svcGetRolesForBundleDrift=db.GetRolesForBundle`.
  - `router.go` registers all six routes under `withCORS(withOperatorAuth(...))`, static `/reconcile` and `/bulk-attribute` segments ordered ahead of/unambiguous against `/{id}/...`.
  - `services/views.go` `governanceFromSnapshot` adds `Drift: models.DriftSummary{Count, Top}` beside the existing pending-propagation block, via new `services/deps.go` injectables `svcCountPendingDrift=db.CountPendingDrift` and `svcGetTopDrift` (GetDriftItems capped to N); count/lookup errors degrade to 0/empty, non-fatal.
  - Tests: `handlers/drift_test.go` (`resetDriftDeps` + 5 tests — mark-external atomic, revoke atomic-then-drain, attribute-to-bundle-missing-role→400, revoke lost-race→409, reconcile-now→sweep). Fixed a latent nil-pointer panic in `services/views_test.go`'s `resetGovernanceDeps` (missing safe-baseline stubs for the new `svcCountPendingDrift`/`svcGetTopDrift` injectables, which fell through to the nil PG pool). Full backend suite green (404 tests), `go vet`/`go build`/`gofmt` clean.
- [x] **Task 17** — Frontend Drift surfaces (Material red, parent design §5.3): top-level `⚠ Drift` nav with persistent red dot; sticky undismissible cross-page banner; undismissible dashboard callout (top-3 + `Triage all →`); `/governance/drift` page with three actions + bulk attribute + filters; motion (respect `prefers-reduced-motion`); optional chime with `localStorage` kill-switch + avatar-menu toggle. *(B2)*
  - Nav badge (`SidebarNav.tsx`), `DriftBanner.tsx`, `DriftCallout.tsx` were already live from an earlier pass. This task completed the remaining surface: `app/governance/drift/page.tsx` (server-gated, mirrors `governance/pending/page.tsx`), `components/drift/DriftTriageClient.tsx` (filters by user/project/source, `[Reconcile now]`, bulk-attribute with multi-select + `{attributed, failed}` result banner), `components/drift/DriftRowActions.tsx` (per-row Attribute/Revoke/Mark-external, split out once the attribute form + bundle-role validation pushed the row past ~120 lines).
  - Bundle attribution validates client-side against `useBundleRolesByBundle` (per-project-scoped `zitadel_role_key` match against the drift's `role_keys`) and disables bundles missing the role in the `<select>` — no fallback to a bare text `source_ref` was needed since `useBundles`/`useBundleRolesByBundle` already existed.
  - Chime: `lib/driftChime.ts` (`isChimeEnabled`/`setChimeEnabled`/`playDriftChime`, `localStorage` key `mkauth-drift-chime`, default on) — a synthesized WebAudio oscillator beep (~120ms), no audio asset. Wired into the existing `DriftBanner.tsx` via a `count`-increase effect (fires once per new-drift transition), gated on `prefers-reduced-motion` inside `playDriftChime`. Toggle is `components/ChimeToggle.tsx`, mounted beside `ThemeToggle` in the sidebar footer card (`Sidebar.tsx`) — this codebase has no avatar-menu popover, `ThemeToggle` itself lives in that sidebar footer card, so the chime toggle mirrors that exact location rather than inventing a popover.
  - Test: `components/drift/DriftTriageClient.test.tsx` (`@vitest-environment jsdom`, `QueryClientProvider` + `NameResolverProvider`, `makeProxyFetch` stubbing `/api/proxy/...`) — asserts rows render, Revoke POSTs `/governance/drift/{id}/revoke`, and attribute-to-bundle disables a bundle lacking the drift role then POSTs the chosen `source_ref` on confirm.
- [x] **Task 18** — Sub-phase-2 verification gate (as Task 10, scoped to 2).
  - `cd backend && go test ./... && go vet ./...` — 404 tests pass, vet clean (no backend files touched this task; confirms no regression).
  - `cd ui && bun run test && bun run lint && bun run build` — 94 tests pass (3 new), lint clean, production build succeeds including the new `/governance/drift` route.
  - `gofmt -l` on the Task-18-brief's backend touch set — zero diff (no Go files were touched by this UI-only task).
  - Migration `000016` up/down symmetry confirmed statically (no throwaway Postgres in this sandbox, as sub-phase 1 Task 10 did): up creates `drift_items` + 2 indexes + `external_grant_exclusions`; down drops all four objects in reverse order, nothing orphaned.

## Sub-phase 3 — Bundle/rule cascade projection (Theme 2 core) — *task-level; plan pending*

- [ ] **Task 19** — Migration `000017_confirmation_mode`: `mapping_rules.confirmation_mode` + `bundles.confirmation_mode` (default `auto`) + `config_settings` seeded `global.default_rule_confirmation_mode='auto'`. *(Theme 2)*
- [ ] **Task 20** — Cascade enqueueing on the six trigger points (parent design §4.7): add/remove user↔bundle, add/remove role↔bundle, rule fire, rule matcher change. `source='bundle'|'rule'` + `source_ref`. Hook `handleAssignBundleToUser`, `handleAddRoleToBundle`, the (new) remove handlers, and the rule create/update path. **Batch the transactional enqueue at 500 rows per `pgx.Tx`** (commit each batch before opening the next) so a large fan-out never holds locks/buffers proportional to the whole cascade. *(Theme 2; see `design.md` §5 batching note)*
- [ ] **Task 21** — Other-source check before revoke (suppress when another bundle/rule/direct grant still covers the triple); already-exists check before add (self-resolve without API call). *(Theme 2)*
- [ ] **Task 22** — Auto vs manual confirmation: `confirmation_mode` read from row → `config_settings` default; auto drains immediately, manual queues; expiry sweeps + lifecycle cascades hardcoded-auto. Policies UI bulk "Set confirmation mode" multi-select; Settings dropdown for the global default; "Recent automated cascades" sidebar element. *(Theme 2)*
- [ ] **Task 23** — Sub-phase-3 verification gate (as Task 10, scoped to 3).

## Cross-cutting documentation (final consolidation — after sub-phase 3, or sub-phase 2 if shipping the 1+2 milestone)

- [ ] **Task 24** — Adopt the new doctrinal sentence (parent design §7.1) in `CLAUDE.md` Key Conventions and `mkauth-core-architecture/design.md`; add **Drift Control** row + flip access-governance reconciliation note in `feature-coverage.md`; Phase 5.5 closure line in `ROADMAP.md`; this change's row in `INDEX.md`; `DRIFT_RECONCILIATION_INTERVAL_HOURS`, `OUTBOX_MAX_RETRIES`, `OUTBOX_RETENTION_DAYS` in `.env.example`. *(D3)*
