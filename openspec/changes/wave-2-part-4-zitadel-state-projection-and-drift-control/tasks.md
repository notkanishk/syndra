# Wave 2 · Part 4 — Tasks

> This is the OpenSpec progress ledger for the whole change. **Sub-phase 1** has a detailed step-by-step (TDD-ordered, exact code + commands) plan at [`docs/superpowers/plans/2026-06-09-wave-2-part-4-phase-1-outbox.md`](../../../docs/superpowers/plans/2026-06-09-wave-2-part-4-phase-1-outbox.md). Sub-phases 2 and 3 are task-level here and get their own writing-plans pass when sub-phase 1 lands. Plan task numbers execute in size-ascending order within each sub-phase; audit IDs are preserved for traceability.

## Sub-phase 1 — Outbox & operator-confirmed propagation (B4/D3) — *detailed plan exists*

- [ ] **Task 0** — Commit OpenSpec scaffolding (this directory + three spec deltas + the sub-phase-1 plan); no source touched
- [ ] **Task 1** — Migration `000015_zitadel_propagation_outbox` (up + down): `pending_zitadel_propagations` table + status/created_at index; `direct_role_grants.source` (full 5-value CHECK enum) + `source_ref`, existing rows backfill to `source='direct'`. Down drops both columns + the table. *(B4/D3)*
- [ ] **Task 2** — `models.PendingPropagation` struct; add `Source`/`SourceRef` to `models.DirectGrant`. *(B4/D3)*
- [ ] **Task 3** — `db/propagations.go`: `InsertPendingPropagation` / `ClaimPendingPropagations` (`FOR UPDATE SKIP LOCKED`) / `MarkPropagationApplied` / `MarkPropagationFailed` / `RequeuePropagation` / `GetPendingPropagations` / `CountPendingPropagations`. Tests for each transition + the claim-skip-locked semantics. *(B4/D3)*
- [ ] **Task 4** — `db.EnqueueDirectGrantPropagation` (one `pgx.Tx`: upsert `direct_role_grants` + insert `audit_logs` + insert outbox row). Rollback-on-any-failure test. Extend `UpsertDirectGrant` signature with `source`/`source_ref`. *(B4/D3)*
- [ ] **Task 5** — `services/propagation/{drain,deps}.go`: `Drain(ctx)` — health pre-flight, claim, already-exists check (grant index → one live `ListUserGrants` per row), per-row dispatch by `op_type`, ACK classification (`2xx`/`AlreadyExists`(409)→applied; `4xx` except `429`/`408`→failed; `5xx`/timeout/`429`/`408`→requeue with `OUTBOX_MAX_RETRIES` halt), and a drain-tail opportunistic prune of terminal rows older than `OUTBOX_RETENTION_DAYS` (default 30). Classifier reads the Zitadel client's typed status, not the error string. Injectables for zitadel health + the three grant wrappers + index lookup + prune. Tests cover each ACK class (incl. `429`→transient and `409`→applied), the already-exists short-circuit, crash-recovery replay, and the prune. *(B4/D3)*
- [ ] **Task 6** — Rewire `access.go:handleUpsertUserDirectGrant` (`POST /api/v1/users/{id}/grants`) to `EnqueueDirectGrantPropagation`; `202 {outbox_id, idempotency_key, status}`. Optional `?apply=true` triggers an inline single-row drain. *(B4/D3)*
- [ ] **Task 7** — Rewire `discovery.go` `handleAssignZitadelGrant` / `handleUpdateZitadelGrant` / `handleRemoveZitadelGrant` through the canonical path: add→enqueue `add`; update→enqueue `replace`; remove→enqueue `revoke`. `update`/`remove` resolve `grantId→(user,project,roles)` via `db.GetGrantIndex` (live `ListUserGrants` fallback). New response shape. `/zitadel/*` URLs preserved as aliases. *(B4/D3)*
- [ ] **Task 8** — `POST /api/v1/propagations/drain` ("Resume now") + `GET /api/v1/propagations?status=pending` (per-row detail for the confirm modal); extend governance summary with a `pending_propagation` block (count, last-queued, zitadel reachable). Router + operator-auth wiring. *(B4/D3)*
- [ ] **Task 9** — Frontend Pending Propagation surfaces (Material amber, parent design §5.2): `usePendingPropagation` React Query hook; nested `Pending [N]` sidebar item under Operations with amber dot; dismissible dashboard callout with reachability + `Resume now` (confirm modal lists rows); inline `[⏱ Awaiting Zitadel][Resume]` tag; single-pulse badge on increase; update `app/zitadel/page.tsx` grant-mutation callers for the new response shape. *(B4/D3)*
- [ ] **Task 10** — Sub-phase-1 verification gate: `go test ./... && go vet ./...` (backend); `bun run lint && test && build` (ui); migration round-trip `up→down 1→up` on `000015`; `gofmt -d` touch set; `mcp__codebase-memory-mcp__detect_changes` + reindex; `openspec validate … --strict`. *(B4/D3)*

## Sub-phase 2 — Drift detection & triage (B2, C6) — *task-level; plan pending*

- [ ] **Task 11** — Migration `000016_drift_queue` (up + down): `drift_items` (+ partial-unique pending index) + `external_grant_exclusions`. *(B2)*
- [ ] **Task 12** — `db/drift.go` + `db/exclusions.go` repositories (upsert/list/resolve drift; insert/check exclusions); models. *(B2)*
- [ ] **Task 13** — Drop `reconciliationSafetyCap` 10 000 → 2 000; widen the expected set to include rule outputs (`expected_via_rule`) and exclusions; keep `computeReconciliationDiff` pure. Tests for the new classification. *(B2)*
- [ ] **Task 14** — `services/drift/{scheduler,sweep,deps}.go` (mirror `services/expiry/`): scheduled sweep on `DRIFT_RECONCILIATION_INTERVAL_HOURS` (default 6); `zitadel_only → drift_items.upsert`; `mkauth_only → outbox re-enqueue`. Wire into `main.go` beside the expiry scheduler with graceful `Done()`. *(B2, C6)*
- [ ] **Task 15** — Real-time webhook drift: in `webhook.go`/`webhook_translate_enrich.go`, a surviving (non-self) `grant_added`/`grant_removed` unmatched by `external_grant_exclusions` + expected set inserts `drift_items(detection_source='webhook')`. Document the C6 cache-miss-then-drift gap as backstopped by the sweep. Tests. *(C6)*
- [ ] **Task 16** — Triage API: `GET /api/v1/governance/drift` (filters: user/project/source); per-item `POST …/attribute` (`external_backfill`|`bundle`|`rule`, source-remap validation), `POST …/revoke` (enqueue `op_type='revoke'`, mark `revoked` when outbox `applied`), `POST …/mark-external` (insert exclusion); bulk-attribute endpoint; `[Reconcile now]`. *(B2)*
- [ ] **Task 17** — Frontend Drift surfaces (Material red, parent design §5.3): top-level `⚠ Drift` nav with persistent red dot; sticky undismissible cross-page banner; undismissible dashboard callout (top-3 + `Triage all →`); `/governance/drift` page with three actions + bulk attribute + filters; motion (respect `prefers-reduced-motion`); optional chime with `localStorage` kill-switch + avatar-menu toggle. *(B2)*
- [ ] **Task 18** — Sub-phase-2 verification gate (as Task 10, scoped to 2).

## Sub-phase 3 — Bundle/rule cascade projection (Theme 2 core) — *task-level; plan pending*

- [ ] **Task 19** — Migration `000017_confirmation_mode`: `mapping_rules.confirmation_mode` + `bundles.confirmation_mode` (default `auto`) + `config_settings` seeded `global.default_rule_confirmation_mode='auto'`. *(Theme 2)*
- [ ] **Task 20** — Cascade enqueueing on the six trigger points (parent design §4.7): add/remove user↔bundle, add/remove role↔bundle, rule fire, rule matcher change. `source='bundle'|'rule'` + `source_ref`. Hook `handleAssignBundleToUser`, `handleAddRoleToBundle`, the (new) remove handlers, and the rule create/update path. **Batch the transactional enqueue at 500 rows per `pgx.Tx`** (commit each batch before opening the next) so a large fan-out never holds locks/buffers proportional to the whole cascade. *(Theme 2; see `design.md` §5 batching note)*
- [ ] **Task 21** — Other-source check before revoke (suppress when another bundle/rule/direct grant still covers the triple); already-exists check before add (self-resolve without API call). *(Theme 2)*
- [ ] **Task 22** — Auto vs manual confirmation: `confirmation_mode` read from row → `config_settings` default; auto drains immediately, manual queues; expiry sweeps + lifecycle cascades hardcoded-auto. Policies UI bulk "Set confirmation mode" multi-select; Settings dropdown for the global default; "Recent automated cascades" sidebar element. *(Theme 2)*
- [ ] **Task 23** — Sub-phase-3 verification gate (as Task 10, scoped to 3).

## Cross-cutting documentation (final consolidation — after sub-phase 3, or sub-phase 2 if shipping the 1+2 milestone)

- [ ] **Task 24** — Adopt the new doctrinal sentence (parent design §7.1) in `CLAUDE.md` Key Conventions and `mkauth-core-architecture/design.md`; add **Drift Control** row + flip access-governance reconciliation note in `feature-coverage.md`; Phase 5.5 closure line in `ROADMAP.md`; this change's row in `INDEX.md`; `DRIFT_RECONCILIATION_INTERVAL_HOURS`, `OUTBOX_MAX_RETRIES`, `OUTBOX_RETENTION_DAYS` in `.env.example`. *(D3)*
