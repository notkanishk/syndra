# Wave 2 · Part 2 — Tasks

> Detailed step-by-step (TDD-ordered, with exact code blocks and commands) lives at [`docs/superpowers/plans/2026-05-12-wave-2-part-2-backend-coherence.md`](../../../docs/superpowers/plans/2026-05-12-wave-2-part-2-backend-coherence.md). The list below is the OpenSpec progress ledger. Plan task numbers are executed in size-ascending order; audit IDs are preserved on the right for traceability.

- [x] **Task 0** — Commit OpenSpec scaffolding (this directory + two spec deltas + plan); then a `gofmt` baseline cleanup for the wave's touch set so subsequent commits stay focused on logic (no-op if the touch set is already clean)
- [x] **Task 1** — Introduce `services.ErrComplexity` sentinel; rewrite `handlers/vault.go:isComplexityError` via `errors.Is`; delete the prefix-sniff helper. *(Audit: B7)*
- [x] **Task 2** — Tighten `handlers/webhook.go`: remove the silent `EventType = "grant_added"` default for internal-shape callers; persist enrichment-incomplete Zitadel grant events to `webhook_events` with new status value `dropped_enrichment_incomplete`. *(Audit: B6, C11, D8)*
- [x] **Task 3** — Add Redis read-through cache for `claim_failure_mode` in `handlers/action.go`; cache key `claim_mode:<projectID>`; TTL `300s` (env-overridable via `CLAIM_MODE_CACHE_TTL_SECONDS`); fall back to cached value on DB error before defaulting to `fail_closed`. *(Audit: C5)*
- [x] **Task 4** — Introduce `auth.Principal` (subject + project-role set) in `internal/auth/jwt.go`; rename `ValidateToken` → `Validate` returning `*Principal`; stash via new `withPrincipal` ctx helper inside `withUserAuth`; rewrite `withOperatorAuth` to read from context; delete standalone `auth.HasProjectRole`. *(Audit: C4)*
- [x] **Task 5** — Split `internal/db/repositories.go` (1303 lines) into 11 domain files inside the same `db` package: `bundles.go`, `rules.go`, `audit.go`, `grants.go`, `access_requests.go`, `claim_profiles.go`, `onboarding.go`, `webhooks.go` (includes grants index), `roles.go`, `intents.go`, `vault.go`. Function bodies, exported names, package-level symbols all bit-identical. *(Audit: B5)*
- [x] **Task 6** — Introduce request-scoped `accessSnapshot` in `services/views.go`; thread through `ListUsers`, `ListApplications`, `ListProjects`, `Topology`, `Governance`/`BundleImpact`; add call-count test asserting `collectUserRoles` is invoked exactly once per user across a single `/api/v1/applications` request. *(Audit: B3)*
- [x] **Task 7** — Verification gate: `go test ./...` + `go vet ./...` (from `backend/`); `gofmt -d` scoped to this wave's touch set from the repo root (see plan Step 7.1 for the full path list — pre-existing drift in untouched files is out of scope, normalised separately in Task 0 Step 0.3); `mcp__codebase-memory-mcp__detect_changes` + reindex affected scope; `openspec validate wave-2-part-2-backend-coherence --strict` from the repo root
- [x] **Task 8** — `INDEX.md` change-log row; `feature-coverage.md` row for the operational properties this wave introduces (cached `claim_failure_mode`; principal-scoped JWT context; observable enrichment-incomplete drops)
